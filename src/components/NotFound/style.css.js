import { style } from "@vanilla-extract/css"

export const styleSection404 = style({
    padding: "9.5rem 0 15rem"
})

export const container404Style = style({
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
})

export const heading404Style = style({
    fontWeight: 600,
    fontSize: "4.9rem",
    lineHeight: "5.9rem",
    marginBottom: "2.4rem",
})

export const text404Style = style({
    fontSize: "1.6rem",
    lineHeight: "2.4rem",
    textAlign: "center",
    maxWidth: "64.1rem",
    marginBottom: "4rem",
})

export const image404Style = style({
    marginTop: "6.4rem"
})